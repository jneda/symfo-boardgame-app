{% extends 'base.html.twig' %}

{% block title %}
  {{ category.label }}
{% endblock %}

{% block body %}
  <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font:
    18px/1.5 sans-serif; } .example-wrapper code { background: #F5F5F5; padding:
    2px 6px; }
  </style>

  <div class="example-wrapper">
    <article>
      <h1>
        {{ category.label }}
      </h1>
      <p>
        {{ category.description }}
      </p>

      {# <p>Durée d'une partie : {{ boardgame.durationMinutes }} minutes</p>
        <p>Âge minimum : {{ boardgame.minAge }}</p>
        <p>Date de publication : {{ boardgame.publicationDate|format_datetime(pattern='y') }}</p>
        <p>Description : {{ boardgame.description }}</p>
        <p>Matériel de jeu : {{ boardgame.contents }}</p> #}

      <a href="{{ path('app_category') }}">&larr; Toutes les catégories</a>
    </article>
  </div>
{% endblock %}
